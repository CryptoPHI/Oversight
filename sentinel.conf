# specify path to mue.conf or leave blank
# default is the same as MonetaryUnit Core
coin_conf=/root/.muecore/mue.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
